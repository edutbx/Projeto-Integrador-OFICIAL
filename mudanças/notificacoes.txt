================================================================
  SISTEMA DE NOTIFICAÇÕES — SOLICITAÇÃO DE CADASTRO
  Projeto: Saúde++
  Data: 14/03/2026
================================================================

VISÃO GERAL
-----------
Foi implementado um sistema de notificações para que médicos possam
solicitar cadastro diretamente pela tela de login, e o gestor do
sistema seja alertado em tempo real no painel administrativo.

Fluxo resumido:
  1. Médico acessa /login e clica em "Solicitar cadastro"
  2. Um modal abre pedindo nome, e-mail, CRM e mensagem
  3. Ao confirmar, uma notificação é salva no banco de dados (MongoDB)
  4. O gestor vê o sino no header com badge vermelho indicando novas notificações
  5. Ao clicar no sino, um painel exibe todas as solicitações com os dados do médico
  6. O gestor marca como lida e realiza o cadastro manualmente


================================================================
  BACKEND
================================================================

ARQUIVOS CRIADOS
----------------

1. backend/.../auth/model/Notificacao.java
   - Entidade MongoDB mapeada para a collection "notificacoes"
   - Campos:
       id            → ID gerado pelo MongoDB
       tipo          → sempre "SOLICITACAO_CADASTRO"
       nomeRemetente → nome do médico que solicitou
       emailRemetente→ e-mail de contato
       crm           → CRM do médico (opcional)
       mensagem      → observação adicional (opcional)
       dataHora      → data e hora do envio (LocalDateTime)
       lida          → false por padrão, true quando o gestor marca

2. backend/.../auth/repository/NotificacaoRepository.java
   - Interface que estende MongoRepository
   - Métodos:
       findAllByOrderByDataHoraDesc() → lista todas ordenadas pela mais recente
       countByLidaFalse()            → conta quantas ainda não foram lidas

3. backend/.../auth/dto/NotificacaoRequest.java
   - DTO (objeto de transferência) que recebe os dados do frontend
   - Campos: nomeRemetente, emailRemetente, crm, mensagem

4. backend/.../auth/controller/NotificacaoController.java
   - Controller REST com 4 endpoints:

       POST   /api/notificacoes              → público, cria a notificação
       GET    /api/notificacoes/admin        → admin, lista todas
       GET    /api/notificacoes/admin/count  → admin, retorna { naoLidas: N }
       PUT    /api/notificacoes/admin/{id}/lida → admin, marca como lida

   - Os endpoints /admin/* exigem ROLE_ADMIN via @PreAuthorize

ARQUIVOS MODIFICADOS
--------------------

5. backend/.../config/SecurityConfig.java
   - Adicionado "/api/notificacoes" na lista de rotas públicas (permitAll)
   - Isso permite que qualquer pessoa (sem login) envie uma solicitação


================================================================
  FRONTEND
================================================================

ARQUIVOS CRIADOS
----------------

6. frontend/src/services/notificacaoService.ts
   - Serviço TypeScript com 4 funções:

       enviarSolicitacao(dados)       → POST /api/notificacoes (público)
       listarNotificacoes(token)      → GET  /api/notificacoes/admin
       contarNaoLidas(token)          → GET  /api/notificacoes/admin/count
       marcarComoLida(id, token)      → PUT  /api/notificacoes/admin/{id}/lida

   - Também exporta a interface Notificacao (tipagem TypeScript)

ARQUIVOS MODIFICADOS
--------------------

7. frontend/src/componets/GestorHeader/GestorHeader.tsx
   - Convertido para componente com estado (useState + useEffect)
   - Adicionado sino de notificações no header do gestor:
       → Badge vermelho com contagem de não lidas
       → Animação de pulso quando há notificações novas
       → Polling automático a cada 30 segundos para verificar novas notificações
       → Painel dropdown ao clicar no sino com lista de solicitações
       → Cada item mostra: nome, e-mail, CRM, mensagem e horário
       → Botão "Marcar como lida" em cada notificação não lida
       → Fecha ao clicar fora do painel

8. frontend/src/styles/components/GestorHeader.css
   - Adicionados estilos para:
       .gestor-bell           → botão do sino
       .gestor-bell--active   → pulso animado quando há notificações
       .gestor-bell__badge    → badge vermelho com número
       .gestor-notif-panel    → painel dropdown
       .gestor-notif-item     → cada notificação na lista
       .gestor-notif-item--lida → estilo desbotado para lidas

9. frontend/src/layout/Login/BodyLogin.tsx
   - Botão "Solicitar cadastro" agora abre um modal (antes redirecionava para /cadastro)
   - Modal contém:
       → Campos: Nome completo*, E-mail*, CRM (opcional), Mensagem (opcional)
       → Botões: Cancelar / Enviar solicitação
       → Estado de loading durante o envio
       → Tela de sucesso com ícone verde após envio confirmado
       → Fecha clicando fora do modal ou no X

10. frontend/src/styles/layout/BodyLogin.css
    - Adicionados estilos para o modal:
        .solicit-overlay       → fundo escurecido com blur
        .solicit-modal         → caixa do modal
        .solicit-modal__sucesso→ tela de confirmação verde
        .solicit-modal__actions→ botões cancelar/enviar


================================================================
  SEGURANÇA
================================================================

  → Qualquer pessoa pode enviar uma solicitação de cadastro
  → Isso é intencional: o médico ainda não tem conta para se autenticar

  → Protegidos por @PreAuthorize("hasRole('ADMIN')")
  → Apenas o gestor autenticado pode ver e gerenciar as notificações


================================================================
  ARQUIVOS ALTERADOS — RESUMO
================================================================

  CRIADOS:
    backend/auth/model/Notificacao.java
    backend/auth/repository/NotificacaoRepository.java
    backend/auth/dto/NotificacaoRequest.java
    backend/auth/controller/NotificacaoController.java
    frontend/src/services/notificacaoService.ts

  MODIFICADOS:
    backend/config/SecurityConfig.java
    frontend/src/componets/GestorHeader/GestorHeader.tsx
    frontend/src/styles/components/GestorHeader.css
    frontend/src/layout/Login/BodyLogin.tsx
    frontend/src/styles/layout/BodyLogin.css

================================================================
================================================================
  SISTEMA DE PACIENTES — CRUD (GESTOR + MÉDICO)
  Projeto: Saúde++
  Data: 11/04/2026
================================================================

VISÃO GERAL
-----------
Foi implementado o CRUD completo de pacientes com os campos:
  - Nome
  - Idade
  - Endereço
  - Altura
  - Peso

Também foi feita a integração para:
  - Gestor cadastrar, buscar, editar e excluir pacientes
  - Médico consultar/listar pacientes

Preparação para próximos passos:
  - Estrutura pronta para vínculo com prontuário
  - Estrutura pronta para restringir visibilidade por médico/consulta no futuro

Fluxo resumido:
  1. Gestor acessa /gestor/pacientes
  2. Gestor cadastra paciente com os dados básicos
  3. Gestor pode buscar, editar e excluir paciente
  4. Médico acessa /medico/pacientes e consulta os pacientes cadastrados
  5. Atualmente todos os médicos visualizam todos os pacientes (regra temporária)


================================================================
  BACKEND
================================================================

ARQUIVOS CRIADOS
----------------

1. backend/.../auth/model/Paciente.java
   - Entidade MongoDB mapeada para a collection "pacientes"
   - Campos principais:
       id, nome, idade, endereco, altura, peso
   - Campos de preparação para evolução:
       medicoCrmReferencia
       medicoCrmsComAcesso (lista de médicos com potencial acesso)
       prontuarioAtualId
       ultimaAlteracaoProntuarioPorCrm
       ultimaAlteracaoProntuarioEm
   - Campos de auditoria:
       criadoPor, criadoEm, atualizadoPor, atualizadoEm

2. backend/.../auth/repository/PacienteRepository.java
   - Interface que estende MongoRepository para persistência dos pacientes

3. backend/.../auth/dto/PacienteRequest.java
   - DTO de entrada para criação/atualização
   - Validações:
       nome/endereco obrigatórios
       idade mínima 0
       altura e peso positivos

4. backend/.../auth/dto/PacienteResponse.java
   - DTO de saída para retorno dos dados do paciente
   - Inclui campos de preparo para prontuário e vínculo médico

5. backend/.../auth/dto/PacienteVinculoMedicoRequest.java
   - DTO para atualização direta do vínculo principal com médico (CRM)

6. backend/.../auth/service/PacienteService.java
   - Regras de negócio do módulo de pacientes:
       criar, listar (com busca), buscarPorId, atualizar, deletar
       atualizarVinculoMedico
   - Busca textual por nome, endereço e CRM de referência

7. backend/.../auth/controller/PacienteController.java
   - Controller REST com endpoints:

       GET    /api/pacientes                     → lista pacientes (admin e médico)
       GET    /api/pacientes/{id}                → detalha paciente por ID (admin e médico)
       POST   /api/pacientes                     → cria paciente (admin)
       PUT    /api/pacientes/{id}                → atualiza paciente (admin)
       PUT    /api/pacientes/{id}/vinculo-medico → atualiza vínculo com médico (admin)
       DELETE /api/pacientes/{id}                → remove paciente (admin)

ARQUIVOS MODIFICADOS
--------------------

8. backend/.../config/SecurityConfig.java
   - Rotas de página protegidas adicionadas:
       /gestor/pacientes (ROLE_ADMIN)
       /medico/pacientes (ROLE_USER)

9. backend/.../auth/controller/appController.java
   - Forward de rotas SPA adicionadas:
       GET /gestor/pacientes
       GET /medico/pacientes


================================================================
  FRONTEND
================================================================

ARQUIVOS CRIADOS
----------------

10. frontend/src/services/pacienteService.ts
    - Serviço TypeScript para consumir API de pacientes
    - Funções:
        listarPacientesComoGestor(busca)
        listarPacientesComoMedico(busca)
        criarPaciente(payload)
        atualizarPaciente(id, payload)
        deletarPaciente(id)

11. frontend/src/layout/GestorPacientes/BodyGestorPacientes.tsx
    - Tela principal do gestor para pacientes
    - Recursos:
        formulário de cadastro/edição
        busca
        listagem em tabela
        exclusão com confirmação

12. frontend/src/pages/GestorPacientes.tsx
    - Página do gestor protegida por autenticação de gestor

13. frontend/src/layout/MedicoPacientes/BodyMedicoPacientes.tsx
    - Tela simples de consulta para médicos
    - Recursos:
        busca e listagem em tabela

14. frontend/src/pages/MedicoPacientes.tsx
    - Página do médico protegida por autenticação de médico

15. frontend/src/styles/layout/BodyGestorPacientes.css
    - Estilos da tela de pacientes do gestor

16. frontend/src/styles/layout/BodyMedicoPacientes.css
    - Estilos da tela de pacientes do médico

ARQUIVOS MODIFICADOS
--------------------

17. frontend/src/App.tsx
    - Novas rotas registradas:
        /gestor/pacientes
        /medico/pacientes

18. frontend/src/types/index.ts
    - Novas interfaces:
        Paciente
        PacientePayload
        PacientesResponse

19. frontend/src/layout/Gestor/BodyGestor.tsx
    - Link "Pacientes" adicionado no menu lateral do gestor

20. frontend/src/layout/GestorMedicos/BodyGestorMedicos.tsx
    - Link "Pacientes" adicionado no menu lateral compartilhado

21. frontend/src/layout/Medico/BodyMedico.tsx
    - Atalho "Consultar Pacientes" adicionado no painel do médico


================================================================
  SEGURANÇA
================================================================

- Endpoints de leitura de pacientes (GET) acessíveis para:
  → ROLE_ADMIN e ROLE_USER

- Endpoints de escrita (POST/PUT/DELETE) restritos para:
  → ROLE_ADMIN

- Resultado prático atual:
  → Gestor gerencia cadastro dos pacientes
  → Médico somente consulta pacientes

- Regra temporária de negócio mantida:
  → Todos os médicos visualizam todos os pacientes

- Preparado para regra futura:
  → Restrição por consulta/médico pode ser implementada usando
    medicoCrmReferencia e medicoCrmsComAcesso


================================================================
  VALIDAÇÃO
================================================================

- Backend compilado com sucesso via Maven local (mvn compile)
- Frontend buildado com sucesso (npm run build)
- Permanece 1 warning antigo de hook em GestorHeader.tsx (pré-existente)


================================================================
  ARQUIVOS ALTERADOS — RESUMO
================================================================

  CRIADOS:
    backend/auth/model/Paciente.java
    backend/auth/repository/PacienteRepository.java
    backend/auth/dto/PacienteRequest.java
    backend/auth/dto/PacienteResponse.java
    backend/auth/dto/PacienteVinculoMedicoRequest.java
    backend/auth/service/PacienteService.java
    backend/auth/controller/PacienteController.java
    frontend/src/services/pacienteService.ts
    frontend/src/layout/GestorPacientes/BodyGestorPacientes.tsx
    frontend/src/pages/GestorPacientes.tsx
    frontend/src/layout/MedicoPacientes/BodyMedicoPacientes.tsx
    frontend/src/pages/MedicoPacientes.tsx
    frontend/src/styles/layout/BodyGestorPacientes.css
    frontend/src/styles/layout/BodyMedicoPacientes.css

  MODIFICADOS:
    backend/config/SecurityConfig.java
    backend/auth/controller/appController.java
    frontend/src/App.tsx
    frontend/src/types/index.ts
    frontend/src/layout/Gestor/BodyGestor.tsx
    frontend/src/layout/GestorMedicos/BodyGestorMedicos.tsx
    frontend/src/layout/Medico/BodyMedico.tsx

================================================================

================================================================
  SISTEMA DE PRONTUÁRIO — CRUD + IA (GESTOR + MÉDICO)
  Projeto: Saúde++
  Data: 12/04/2026
================================================================

VISÃO GERAL
-----------
Foi implementado o CRUD de prontuário com fluxo entre gestor e médico,
incluindo integração com IA para interpretação do resumo clínico.

Fluxo resumido:
  1. Gestor acessa a tela de pacientes e entra no prontuário do paciente
  2. Gestor cria o prontuário inicial no contexto do paciente selecionado
  3. Médico acessa Nova Consulta, seleciona o paciente e abre o prontuário
  4. Médico atualiza os campos clínicos durante o atendimento
  5. Médico pode enviar o resumo para interpretação por IA


================================================================
  BACKEND
================================================================

ARQUIVOS CRIADOS
----------------

1. backend/.../auth/model/Prontuario.java
   - Entidade MongoDB mapeada para a collection "prontuarios"
   - Campos principais:
       id, pacienteId, nomePaciente, idadePaciente, enderecoPaciente
       alturaPaciente, pesoPaciente
       resumoProblema, historico, diagnostico, tratamento, observacoes
       criadoPor, criadoEm, atualizadoPor, atualizadoEm

2. backend/.../auth/dto/ProntuarioRequest.java
   - DTO de entrada para criação e atualização do prontuário

3. backend/.../auth/dto/ProntuarioResponse.java
   - DTO de saída para retorno completo do prontuário

4. backend/.../auth/repository/ProntuarioRepository.java
   - Repositório Mongo com busca por pacienteId

5. backend/.../auth/service/ProntuarioService.java
   - Regras de negócio do prontuário:
       criarParaPaciente, buscarPorPacienteId, buscarPorId,
       atualizar, interpretarComIa, deletar
   - Atualiza referência no paciente:
       prontuarioAtualId, ultimaAlteracaoProntuarioPorCrm, ultimaAlteracaoProntuarioEm

6. backend/.../auth/controller/ProntuarioController.java
   - Controller REST com endpoints:

       POST   /api/prontuarios/paciente/{pacienteId}              → cria prontuário (admin)
       GET    /api/prontuarios/paciente/{pacienteId}              → busca por paciente (admin e médico)
       GET    /api/prontuarios/{id}                               → busca por ID (admin e médico)
       PUT    /api/prontuarios/{id}                               → atualiza (admin e médico)
       POST   /api/prontuarios/{id}/interpretar-ia                → interpretação por IA (admin e médico)
       DELETE /api/prontuarios/{id}                               → remove prontuário (admin)

ARQUIVOS MODIFICADOS
--------------------

7. backend/.../auth/controller/appController.java
   - Forward de rota SPA adicionada:
       GET /gestor/pacientes/prontuario

8. backend/.../config/SecurityConfig.java
   - Rota protegida adicionada:
       /gestor/pacientes/prontuario (ROLE_ADMIN)


================================================================
  FRONTEND
================================================================

ARQUIVOS CRIADOS
----------------

9. frontend/src/services/prontuarioService.ts
   - Serviço TypeScript para API de prontuário
   - Funções:
       criarProntuarioParaPaciente(pacienteId, payload)
       buscarProntuarioPorPacienteId(pacienteId)
       buscarProntuarioPorId(id)
       atualizarProntuario(id, payload)
       interpretarProntuarioComIa(id)
       deletarProntuario(id)

10. frontend/src/pages/GestorPacienteProntuario.tsx
    - Página protegida do gestor para criação/edição de prontuário do paciente

11. frontend/src/layout/GestorPacienteProntuario/BodyGestorPacienteProntuario.tsx
    - Formulário de prontuário no contexto do paciente selecionado

12. frontend/src/styles/layout/BodyGestorPacienteProntuario.css
    - Estilos da tela de prontuário do gestor

ARQUIVOS MODIFICADOS
--------------------

13. frontend/src/App.tsx
    - Nova rota registrada:
        /gestor/pacientes/prontuario

14. frontend/src/types/index.ts
    - Novas interfaces:
        Prontuario
        ProntuarioPayload

15. frontend/src/services/pacienteService.ts
    - Adicionado método de busca por ID para fluxo do prontuário do gestor

16. frontend/src/layout/GestorPacientes/BodyGestorPacientes.tsx
    - Botão/ação para abrir prontuário do paciente

17. frontend/src/layout/NovaConsulta/BodyNovaConsulta.tsx
    - Fluxo alterado para seleção de paciente antes de abrir prontuário

18. frontend/src/layout/Prontuario/BodyProntuario.tsx
    - Tela refatorada para consumo real da API de prontuário:
      edição, salvamento e interpretação por IA

19. frontend/src/styles/layout/BodyNovaConsulta.css
    - Estilos da listagem/seleção de pacientes na consulta

20. frontend/src/styles/layout/BodyProntuario.css
    - Estilos para novo formulário, estado de erro e retorno da IA


================================================================
  SEGURANÇA
================================================================

- Endpoints de leitura/edição clínica do prontuário acessíveis para:
  → ROLE_ADMIN e ROLE_USER

- Endpoints de criação e exclusão de prontuário restritos para:
  → ROLE_ADMIN


================================================================
  VALIDAÇÃO
================================================================

- Backend compilado com sucesso (mvn compile)
- Frontend buildado com sucesso (npm run build)
- Integração de IA mantida via endpoint local de Python (/chat)


================================================================
  ARQUIVOS ALTERADOS — RESUMO
================================================================

  CRIADOS:
    backend/auth/model/Prontuario.java
    backend/auth/dto/ProntuarioRequest.java
    backend/auth/dto/ProntuarioResponse.java
    backend/auth/repository/ProntuarioRepository.java
    backend/auth/service/ProntuarioService.java
    backend/auth/controller/ProntuarioController.java
    frontend/src/services/prontuarioService.ts
    frontend/src/pages/GestorPacienteProntuario.tsx
    frontend/src/layout/GestorPacienteProntuario/BodyGestorPacienteProntuario.tsx
    frontend/src/styles/layout/BodyGestorPacienteProntuario.css

  MODIFICADOS:
    backend/auth/controller/appController.java
    backend/config/SecurityConfig.java
    frontend/src/App.tsx
    frontend/src/types/index.ts
    frontend/src/services/pacienteService.ts
    frontend/src/layout/GestorPacientes/BodyGestorPacientes.tsx
    frontend/src/layout/NovaConsulta/BodyNovaConsulta.tsx
    frontend/src/layout/Prontuario/BodyProntuario.tsx
    frontend/src/styles/layout/BodyNovaConsulta.css
    frontend/src/styles/layout/BodyProntuario.css

================================================================
================================================================
  TESTES AUTOMATIZADOS — PACIENTE E PRONTUÁRIO
  Projeto: Saúde++
  Data: 12/04/2026
================================================================

VISÃO GERAL
-----------
Foram implementados e executados testes automatizados para os módulos
de paciente e prontuário, cobrindo regras de negócio e controle de acesso.


================================================================
  BACKEND — TESTES CRIADOS
================================================================

1. backend/src/test/java/com/br/iasaude/saudemais/auth/service/PacienteServiceTest.java
   - Testes unitários do serviço de paciente
   - Cobertura de cenários de criação, busca, atualização e remoção

2. backend/src/test/java/com/br/iasaude/saudemais/auth/service/ProntuarioServiceTest.java
   - Testes unitários do serviço de prontuário
   - Cobertura de cenários de criação, busca, atualização, IA e remoção

3. backend/src/test/java/com/br/iasaude/saudemais/auth/controller/ProntuarioControllerTest.java
   - Testes de controller com MockMvc
   - Validação de autorização por perfil (ADMIN/USER), respostas 200/403/404


================================================================
  RESULTADO DA EXECUÇÃO
================================================================

- Comando executado: mvn test
- Resultado final:
  → Tests run: 28
  → Failures: 0
  → Errors: 0
  → BUILD SUCCESS

================================================================
